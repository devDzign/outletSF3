{% extends '::layout/layoutAdmin.html.twig' %}

{% block body %}
    <h2>Pages edit</h2>
    <div class="span4">
        {{ form(edit_form) }}
    </div>
    <div class="span8">
        <div class="row">
            <div class="span6">
                <a href="{{ path('admin_pages_index') }}" class="btn btn-primary">Retour</a>| {{ form(delete_form) }}
            </div>
        </div>
    </div>
{% endblock %}
