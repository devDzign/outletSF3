{% extends '::layout/layoutAdmin.html.twig' %}

{% block body -%}
    <h1>Produits creation</h1>

    {{ form(form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('admin_produits_index') }}" class="btn btn-primary">Back to the list</a>
        </li>
    </ul>
{% endblock %}
